# CLAUDE.md — 프로젝트 작업 지침

> 이 파일은 GitHub에서 `@claude`가 호출될 때(또는 로컬 Claude Code에서) 따르는 규칙입니다.
> 친구가 이슈/PR에 `@claude ...`로 요청하면 Claude가 아래 규칙에 맞춰 수정하고 PR을 올립니다.

> # ▶ 지금 할 일: **US 계수를 며칠 지켜보는 것** (2026-08-13 결정 · 아래 ⏱)
>
> ## ⏱ 며칠 지켜볼 것 — US 계수가 스스로 회복하는가
>
> **⚠ 먼저: 그동안 `Coefficients` 워크플로는 매번 빨간불로 끝납니다. 고장이 아닙니다.**
> US를 못 만들어 이전 파일을 이어받을 때마다 일부러 실패로 끝내도록 만들었습니다
> (ADR-0049). **서비스는 정상입니다** — 라이브가 쓰는 US 계수는 2026-08-12 20:40 로컬
> 빌드이고 유효합니다.
>
> **무엇이 문제인가**: 야후가 **GitHub 러너 IP**에서 `Ticker().info`에 빈 값을 줍니다.
> 오류조차 안 나고 조용히 빕니다. 같은 코드가 **집 PC에서는 정상**입니다
> (AAPL per=34.66 · MSFT per=28.09 — 2026-08-13 확인). yfinance 1.5.1 + curl_cffi는 이미
> 브라우저 위장을 하므로 지문이 아니라 **IP** 문제입니다.
>
> **왜 US만 전멸하나 — 구조입니다.**
>
> | | per·pbr | psr·ev_ebitda | 야후가 죽으면 |
> |---|---|---|---|
> | KR | 네이버 | 야후 | 기둥 둘은 산다 → **얇아질 뿐** |
> | US | 야후 | 야후 | **다리 0개** |
>
> US는 네 다리가 전부 `_info_metrics` **한 호출**에 매달려 있습니다. 받쳐 주는 원천이 없습니다.
>
> **간헐적입니다 — "항상"이 아닙니다**: 2026-08-12 06:17 ✅ · 19:43 ✅ · 20:48 ❌ ·
> 21:35 ❌ · 21:38 ❌ · 21:40 ❌ · 08-13 03:30 ❌. 앞의 넷은 한 시간에 빌드를 네 번 돌려
> 자초한 면이 있지만 **03:30은 무관합니다.**
>
> **그래서 기다립니다**(사용자 결정 · 2026-08-13). ADR-0051의 캐시가 붙었으므로 **US가 한
> 번만 성공하면 그 값이 캐시에 쌓이고, 이후 막혀도 유지됩니다** — KR이 정확히 그렇게 3회에
> 수렴했습니다. 하루 두 번 시도하니 06:17 같은 창을 잡을 가능성이 있습니다.
>
> ### 무엇을 보면 되나
>
> Actions → `Coefficients` → 각 실행 로그의 `Build coefficients` 단계:
>
> - **좋음**: `[확인] US: 표본 1,506행 · ev_ebitda=… · psr=…` 이 **한 번이라도** 뜬다.
>   그러면 캐시에 쌓였고, 그 뒤 실패해도 유지됩니다.
> - **나쁨**: 계속 `[문제] US: … (얻은 다리: 없음 · 스냅숏 1,506행)`.
>   **3~4일 뒤에도 그렇다면 기다림이 답이 아닙니다** → 아래 다음 수로 갑니다.
>
> **KR도 처음 2~3회는 얇게 나오는 것이 정상입니다.** GitHub 캐시는 브랜치별이라 main은
> 처음부터 쌓습니다 — `ev_ebitda` 550~1,000대로 시작해 3회쯤 1,300대면 제대로 수렴한 것입니다
> (실측: 557 → 1,006 → 1,379). **얇다고 놀라지 마세요.** ADR-0050의 문턱이 그동안 두꺼운
> 계수를 지킵니다.
>
> ### 안 되면 다음 수 (지금 하지 마세요)
>
> **US에도 받쳐 주는 원천을 붙입니다** — 네 다리가 한 호출에 매달린 구조를 깹니다.
> PER·PBR·PSR은 주가 × 주식수 + 재무제표로 계산할 수 있고 그 데이터는 다른 경로로 옵니다.
> KR이 네이버로 per·pbr를 받치는 것과 같은 모양입니다. **일이 크므로 위 관찰 결과를 보고
> 시작하세요.**
>
> ---
>
> **✅ KR 계수 사고는 끝났습니다** (PR #164·#165 ·
> [ADR-0049](docs/adr/0049-the-build-must-not-destroy-what-it-could-not-rebuild.md) ·
> [ADR-0050](docs/adr/0050-a-thin-build-must-not-replace-a-thick-one.md)).
>
> 무슨 일이었나: 밤 워크플로가 KRX 차단(`Access Denied`)으로 KR을 못 만들었는데 **exit 0**
> 으로 끝났고, publish의 **orphan force-push가 멀쩡하던 어제치 `KR.json`까지 지웠습니다.**
> 스크립트 주석은 *"한쪽 실패로 양쪽을 다 버리면 안 된다"*고 **정확히 반대 의도**를 적고
> 있었는데, 저자가 publish를 덧쓰기로 봤지만 실제로는 교체였습니다.
>
> 고친 방식은 **"실패가 데이터를 파괴하지 않게"**입니다 — 차단이 상시가 아니라 **가끔**이기
> 때문입니다(같은 러너에서 06:17 성공 · 19:43 실패). 워크플로가 브랜치 내용을 먼저 깔고
> 그 위에 덮어쓰며, 못 만든 시장은 어제 파일이 남고 **빨간불로 알립니다**.
> ADR-0050이 여기에 하나 더 얹었습니다 — 모양만 맞고 **표본이 얇은** 계수도 거부합니다
> (기둥 다리 `per`·`pbr`가 이전의 90% 미만이면).
>
> **실전 검증됨**: 워크플로를 손으로 돌렸더니 이번엔 US가 실패했고, **이어받기가 작동해
> `US.json`이 살아남았으며 빨간불이 떴습니다.** 옛 코드였으면 지워졌을 자리입니다.
>
> **✅ Render는 KRX에 막히지 않습니다** (2026-08-13 · 사용자가 Render Shell에서 확인).
> `curl -o /dev/null -w "%{http_code}" http://data.krx.co.kr/comm/bldAttendant/executeForResourceBundle.cmd`
> → **200**. 그래서 최악은 *'판정이 조용히 바뀜'이 아니라 '배포마다 263초'*이고, 런타임 KR
> 경로(`get_kr_listing` — 피어 선정·시총·업종 라벨)도 안전합니다.
> ⚠ **한 시점의 한 번**입니다 — 차단은 GitHub 러너에서도 가끔이었으므로 "영원히 안 막힌다"가
> 아닙니다.
>
> ## ⚠ 열어 둔 질문 — 계수의 재적합 잡음 (혼자 정하지 마세요)
>
> ADR-0050의 문턱을 정하려고 재다가 나온 것이고, **문턱보다 큰 이야기입니다.**
> 같은 스냅숏에서 **표본을 20%만 바꿔도 적정가가 중앙값 9.5% 움직입니다**
> (`python scripts/check_coefficient_thinning.py --sweep`). 무작위 부분표본과 "그날 수집에
> 성공한 종목 집합"은 같은 종류의 변동이므로, 이는 **매일 재적합하는 계수가 날마다 그만큼
> 흔들린다**는 뜻이기도 합니다.
>
> 즉 화면의 적정가에 **값 매기지 않은 오차**가 있습니다. ADR-0017이 표시하는 추정 오차는
> 회귀의 **예측** 오차(`LEG_MAE`)이지 **계수 자체의 표본 변동**이 아닙니다.
>
> **여기서 숫자를 정하지 마세요.** 이 저장소가 여러 번 데인 자리입니다(ADR-0041의 가중치 폭 ·
> ADR-0042의 판정 문턱 — 둘 다 결과를 본 뒤 자를 고른 이해충돌이 남았습니다).
> 열려면 **먼저 사전등록**하세요: 무엇을 오차로 부를지 · 어떤 자로 잴지 · 화면에 무엇을
> 바꿀지를 재기 전에 적습니다. 측정 자체의 한계도 그대로입니다 — 종목 18개 표본이고,
> 무작위 부분표본은 실제 수집 실패(소형주가 먼저 빠지는 식)와 같지 않습니다.
>
> ---
>
> ## ✅ 콜드 스타트 작업이 끝났습니다 (PR #159·#160·#161·#162 · ADR-0046·0047·0048)
>
> 무엇을 하든 **`python scripts/check_load_timing.py KR 005930`을 먼저 돌려 지금 표를
> 뜨세요** — 숫자는 캐시 상태에 따라 매번 다릅니다(주장하는 것은 숫자가 아니라 모양입니다).
>
> **첫 방문자가 겪는 시간**:
>
> | 상황 | 전 | 후 |
> |---|---|---|
> | 배포 직후 · 쇼케이스 종목 | 12.33초 | **2.46초** |
> | 배포 직후 · 쇼케이스 밖 | 12.33초 | 7.89초 |
> | 웜 (같은 종목 재조회) | 1.4~2.0초 | 그대로 |
>
> ⚠ **7.89초는 #160(피어 병렬화) 이전 브랜치에서 잰 값입니다.** 넷을 다 머지한 조합은
> 재지 않았습니다 — 인용하기 전에 다시 뜨세요.
>
> **㉠ 피어 보정 루프를 병렬로**([ADR-0046](docs/adr/0046-the-peer-loop-was-the-last-queue.md)).
> ADR-0045가 *"캐시가 있고 실측 0.01초라 두었다"*고 남긴 자리인데, **캐시가 빈 상태는
> 재지 않았던 것**이 문제였습니다. 콜드에서 3.60초(전체의 29.2%)였고, 세 측정이
> −2.4~2.8초로 수렴합니다. 페이로드 완전 일치(KR 005930·000660).
>
> **㉡ 파이프라인 single-flight**([ADR-0047](docs/adr/0047-the-pipeline-cache-did-not-hold-a-lock.md)).
> `_pipeline`의 `@lru_cache`는 호출 **중에** 락을 잡지 않아 동시 호출자 4명에 `_load`가
> **4번** 불렸습니다. ⚠ **다만 옛 인계문의 "수집이 두 벌 돕니다"는 과장이었습니다** —
> 원천 20개가 전부 `@file_cache`라 네트워크는 대략 한 벌이고, 두 벌 도는 것은 그 위
> (스레드풀·계산·직렬화)입니다. **이 변경이 사는 것은 시간이 아니라 부하입니다.**
> 덤으로 **AI 버튼의 캐시 적중이 한 번도 없었던 것**을 찾았습니다 — `analyze()`는 7인자,
> AI 헬퍼는 5인자로 같은 함수를 불러 lru_cache가 별개 항목으로 봤습니다.
>
> **㉢ 기동 직후 예열**([ADR-0048](docs/adr/0048-warm-the-showcase-on-boot-not-a-disk.md)).
> Render 파일시스템이 휘발성이라 **배포마다 캐시가 통째로 빕니다.** `_warm_cache`가
> 계수 → 쇼케이스 종목·금리 순으로 채웁니다(실측 56초, 서버가 냄).
> ⚠ **디스크는 일부러 안 붙였습니다** — 인스턴스가 하나로 묶이고 **무중단 배포가 사라집니다.**
> 붙이려 하기 전에 ADR-0048의 '검토한 대안'을 읽으세요.
>
> **㉣ 화면·프롬프트가 손으로 적고 있던 방법 구성**(PR #159). AI 프롬프트가 `①②③`을
> 하드코딩해 **모델에게 틀린 구성을 먹이고 있었습니다.** 판정은 ①③⑤입니다.
>
> **⚠ 로그 문자는 cp949로 인코딩 가능해야 합니다.** `src/web/prewarm.py`의 `✓`가
> `대시보드실행.bat`(윈도우 콘솔)에서 `UnicodeEncodeError`를 냈고, 그 예외는 `log()`
> 자리라 try 밖이었습니다 — **예열 스레드에서 터지면 첫 종목 뒤로 전부 조용히 죽습니다.**
> `tests/test_prewarm.py`가 지킵니다.
>
> **손대지 않은 것 하나** — **Gemini를 화면 밖으로 미루기**(옛 인계문 §6).
> **범위 밖입니다. 혼자 정하지 마세요.**
>
> ⚠ **다만 여기 적혀 있던 근거는 사실과 달랐습니다**(2026-08-13 코드 확인 · ADR-0052).
> *"판정 가중의 38.5%가 걸린 ①이 나중에 채워져 판정 숫자가 한 번 바뀐다"*고 적혀 있었는데,
> **계수가 있으면 ①은 바뀌지 않습니다.** ①⑤의 적정 배수는 `regression_sector(d)` —
> 즉 **거래소 공식 분류**로 조회하고(ADR-0044), `warranted_multiple`의 입력은
> `(계수, 시총, 거래소업종, ROE)`라 **피어도 AI 라벨도 들어가지 않습니다.**
> 미루면 바뀌는 것은 **피어 표와 업종 상대점수**이고, ①은 **계수가 없는 폴백에서만** 걸립니다.
>
> 그래도 혼자 정하지 마세요 — 화면이 두 번에 걸쳐 채워지는 것은 성능이 아니라
> "단정하지 않는다"는 성격 문제이고, 그 판단은 사용자 몫입니다.
>
> ---
>
> **✅ 첫 조회를 병렬화했습니다** ([ADR-0045](docs/adr/0045-parallelize-the-load-path.md)).
> **순수 계산은 0.05초였고 나머지는 전부 서로 독립인 네트워크 호출이 줄을 서 있던 것**이었습니다.
> 실측(같은 종목·같은 캐시 상태 A/B): **KR warm 4.22s → 2.10s · KR 뉴스 캐시 끊김
> 5.68s → 2.98s · US warm 4.00s → 1.86s.** 새 개념은 없습니다 — `build_peer_table`의
> 스레드풀과 `file_cache`의 키 단위 락이 하던 것을 로드 경로 나머지에 폈습니다.
>
> **합격선은 "페이로드가 한 바이트도 안 바뀐다"였고 양방향으로 확인했습니다** —
> `python scripts/check_payload_parity.py save|compare KR 005930`. 순차 골든 → 병렬
> 실행에서 `news` 밖 전부 일치, 병렬 골든 → 순차 실행에서 **완전 일치**(KR·US).
> ⚠ 이 검사는 살아 있는 원천을 씁니다 — 캐시 TTL(시세 1시간·뉴스 6시간)이 만료되면
> 두 번째 실행이 새 값을 받습니다. **save와 compare를 같은 시간대에 돌리세요.**
>
> **⚠ 인계문 본문 두 곳이 사실과 달랐습니다**(그 문서 머리에 표로 남겼습니다).
> ㉠ *"산업 뉴스 질의가 거래소 라벨이 된다 — 행동이 바뀌는 유일한 지점"* → **안 바꿨습니다.**
> 그 라벨은 ADR-0013이 AI 분류를 넣은 바로 그 이유라(삼성전자 = '통신 및 방송 장비 제조업')
> 교환을 받을 이유가 없었습니다. 셋 중 둘만 미리 받고 산업 질의는 조인 뒤에 남깁니다.
> ㉡ *"KR·US가 같은 그림"* → **US는 안 됩니다.** `_ai_classify_us`(이름·업종)와
> `extract_ttm`(주식수)이 전부 `fetch_info_metrics`에 걸려 있어 info를 먼저 받습니다.
>
> **⚠ `progress`는 `threading.local()`입니다.** 워커에서 진행 표시가 침묵하므로
> `progress.bind_reporter`로 심어 주고 **끝나면 지웁니다**(풀 워커는 재사용됩니다).
> 새 병렬 코드를 쓸 때 이걸 빠뜨리면 화면이 조용해집니다.
>
> **⚠ 경고 순서는 취향이 아니라 계약입니다.** `d.warnings`는 화면 문구 순서이자
> `serialize._peers`가 "피어 기준"을 찾는 목록입니다. 완료 순서로 붙이면 매번 달라집니다 —
> `tests/test_load_parallel.py`가 이 순서를 지킵니다(Red-Green 확인).
>
> **~~손대지 않은 것 셋~~ — ㉠㉡은 끝났습니다**(ADR-0047·0046 · 위 참조).
> ㉢ Gemini 미루기만 남았고, 그것도 **범위 밖**입니다.
>
> **⚠ 회귀의 업종 라벨이 새고 있었습니다 — 고쳤습니다**
> ([ADR-0044](docs/adr/0044-the-regression-sector-label-must-be-the-exchange-one.md) · PR #155).
> ①⑤의 적정 배수 회귀는 **KRX/GICS 라벨로 적합된 계수표**를 문자열로 조회하는데,
> Gemini 업종분류가 성공하면 `d.sector`가 AI가 지은 이름('반도체')으로 덮여
> **계수표에 없어 조용히 '기타'로 떨어졌습니다.** 캐시된 AI 라벨 65개 중 계수표와 맞는 것이
> **2개**뿐이었고 대형주 10종목이 **전부** '기타'로 갔습니다(SK하이닉스 적정 PER 29.93 → 11.96 ·
> 적정가 +94.8% 차이). **판정의 77%(①38.5+⑤38.5)가 걸린 경로입니다.**
>
> **가장 나쁜 것 — 채점받은 계산과 돌아가던 계산이 달랐습니다.** `backtest_collect.py`는
> `r.Sector`(KRX)를 쓰고 Gemini를 타지 않습니다. 즉 **ADR-0028이 잰 것은 KRX 라벨판**이고
> 화면은 '기타'판이었습니다.
>
> 고친 방법: `CompanyData.sector_official`을 갈라 **회귀·금융업 판별은 공식 라벨**,
> **표시·피어·프롬프트는 AI 라벨**을 쓰게 했습니다. `regression_sector(d)` 한 곳으로 모았고
> **백테스트 폴백(`or d.sector`)을 없애면 안 됩니다** — 없애면 패널이 전부 '기타'가 됩니다.
> 확인: `python scripts/check_sector_label.py --live 005930 000660`
>
> **⚠ 판정 등급이 전 종목에서 얼마나 움직이는지는 재지 않았습니다** — 대형주 10개에서 0개
> 바뀌었지만(ADR-0042의 넓은 문턱이 흡수) 소형주·`too_small` 경로는 안 봤습니다.
> "판정은 안 바뀐다"고 인용하지 마세요.
>
> **✅ 신뢰도 배지를 뗐습니다** ([ADR-0043](docs/adr/0043-the-confidence-badge-did-not-do-what-it-claimed.md) · PR #154).
> 등급이 주장하는 것을 하지 못했습니다 — 한국 '높음' **0건**, **미국은 16,598건 전부
> '낮음'**(등급이 아니라 상수였습니다). 두 등급이 모두 채점된 시점만 견주면 '낮음'이
> 네 자 · 두 지표 모두에서 더 잘 맞습니다(5년 IC 0.229 vs 0.184 · 간격 56.0% vs 25.2%).
> **잰 값(dispersion·n_eff)은 `근거 보기` 안에 숫자로 남습니다.**
> `python scripts/check_confidence_horizon.py [--market US]`가 그 표를 다시 냅니다.
>
> **⚠ 인계문의 표는 방향만 재현됐고 수치는 재현되지 않았습니다.** 등급 분포는 한 건도
> 안 틀렸는데 IC·간격 수치가 다르고, **원인을 특정하지 못했습니다**(인계문에 재현 수단이
> 없었습니다). 옛 4축 가중으로 시점 정렬 없이 돌리면 3·5년에서 **부호까지 뒤집힙니다.**
> 그만큼 자의 선택에 민감하니 이 숫자를 인용할 때 ADR-0043의 해당 절을 함께 읽으세요.
>
> **⚠ 대체 지표를 즉석에서 만들지 마세요** — 사전등록이 먼저입니다.
>
> **✅ README·사용설명서를 지금 화면에 맞췄습니다** (PR #154).
> **사용설명서는 두 벌이었습니다** — 인계문이 말한 저장소 밖 파일(266줄)은 낡은 사본이었고,
> 유지되던 것은 `docs/사용설명서.md`(238줄 · 테스트가 내용을 검사)였습니다. 한 벌로 합쳤고
> 바깥 파일은 포인터만 남겼습니다. **스트림릿은 접지 않습니다**(사용자 결정 · 2026-08-12) —
> 지금은 두고 문서에만 "이 설명은 스트림릿 기준"이라고 표시했습니다.
>
> **⚠ 문서 셋이 "백테스트를 못 한다"고 적고 있었습니다.** ADR-0028이 이미 채점했는데
> README·사용설명서·`stock.html` 주석이 *"주가 데이터가 총 5년뿐"*을 그대로 적고 있었습니다.
> 셋 다 정정했습니다. **백테스트 탭은 계속 닫아 둡니다** — 이유는 데이터 길이가 아니라
> '판정과 다른 계산을 검증한다'이고, 그 탭이 검증하던 ②는 판정에서 아예 빠졌습니다(ADR-0035).
> 확인 완료: `data-tab="backtest"` 버튼은 **HTML 주석 안**에 있습니다(조건부 숨김이 아닙니다).
>
> **D-6단계(GP/A)는 선택 사항으로 남아 있습니다** —
> [`docs/HANDOFF-AXES.md`](docs/HANDOFF-AXES.md)의 6단계. 1~5단계는 끝났거나 접혔습니다.
>
> **✅ 5단계(판정 문턱)는 끝났습니다** ([ADR-0042](docs/adr/0042-verdict-thresholds-what-passed-was-selectivity.md)).
> **판정이 5등급 → 3등급**이 됐고 문턱이 로그 ±0.897로 넓어졌습니다(저평가 gap ≥ +145%).
> 3년·5년 자를 **둘 다** 사전등록해 놓고 쟀고 둘 다 통과했습니다(간격 +21.6%→+31.5% ·
> +33.4%→+48.4%). 덤으로 **현행 5등급이 5년에서 최고 등급이 2등급보다 나빴던 것**도 나왔습니다.
>
> **⚠ 그런데 이긴 이유가 문턱이 아닙니다.** 같은 비중을 gap 순위로만 잘라도 간격이 거의
> 같았습니다(3년 +1.2%p · 5년 −0.1%p). 이긴 것은 **'극단을 좁게 잡은 것'** 자체이지
> 오차에 맞춘 문턱이 아닙니다. **"문턱을 실측 오차에 교정했다"고 인용하면 안 됩니다.**
> **'적정 수준'이 70%가 됩니다** — 도구가 훨씬 말을 덜 합니다(ADR-0017이 예측한 그대로).
>
> **✅ 4단계(가중치 폭)는 끝났습니다** ([ADR-0041](docs/adr/0041-weight-width-is-measured-per-stock.md)).
> 가중치 숫자는 그대로고, 폭은 **전 종목 상수가 아니라 종목마다 실제 범위**로 접힘 안에
> 뜹니다. 삼성전자 −6.7%~+15.0% · KB금융 −4.8%~+10.8%.
>
> **⚠ 인계문이 적어 둔 ±6%도, 그것을 고친 10.9%/15.3%도 쓰지 마세요.**
> 그 두 값을 낸 탐침(동일가중 · ③가중 2배)이 **재정규화하면 문헌 순위를 어깁니다** —
> ③가중 2배는 ③을 최상위로 **역전**시킵니다. 게다가 10.9%/15.3%은 **재현되지 않았습니다**
> (같은 패널에서 4.09%/5.75%). 불일치는 ADR-0041에 남겼고 원인은 못 밝혔습니다.
> `python scripts/check_weight_width.py`가 매번 다시 냅니다.
>
> **⛔ 3단계(`per_q` 이관)는 접었습니다 — 다시 집지 마세요**
> ([ADR-0038](docs/adr/0038-street-targets-are-not-an-answer-key.md) ·
> [측정](docs/review/2026-08-10-forward-multiple-측정.md)).
> 재봤고 **잴 수 있는 자가 없다**는 것이 결론입니다. ㉠ 증권가 목표주가는
> **이 도구가 판정에서 일부러 뺀 것**이라(ADR-0006) 답안지로 쓸 수 없고, ㉡ 현재가는
> 자기 과거 PER에 구조적으로 유리합니다. **두 자가 반대 방향으로 오염돼 있습니다.**
> **현행 규칙의 근거도 함께 무효입니다** — `_forward_value` 도크스트링의 *"목표가 내재
> 멀티플과 +2% 일치"*가 같은 자로 잰 값이라 경고를 달아 뒀습니다.
> 다시 열려면 **답안지부터** 정하세요(깨끗한 것은 미래 수익률뿐인데 시점별 컨센서스가
> 패널에 없습니다).
>
> **그 문서 하나만 읽고 시작할 수 있게 써 뒀습니다.** 아래 배경은 그 다음에 읽으세요.
> 결정된 것 넷 — 다시 논의하지 않습니다:
> **② 역사적 밴드를 판정에서 뺀다**(차트는 남긴다) · **가중치 숫자는 안 바꾸고 폭만
> 보여준다** · **근거·보험성 문구는 첫 화면에서 빼고 눌러서 보게 한다** ·
> **F-Score는 무기한 보류**([ADR-0034](docs/adr/0034-fscore-predicts-but-failed-its-own-gate.md)).
> **다음 ADR 번호는 0053.** (0038 street-targets · 0039 rim-persistence · 0040 roe-buckets가
> 서로 다른 브랜치에서 동시에 잡혔습니다 — `check_adr_index.py`는 브랜치를 가로질러 보지
> 못하므로 **새 ADR을 쓰기 전에 다른 워크트리의 `docs/adr/`를 확인하세요.**
> 2026-08-13에도 0046·0047·0048을 세 브랜치가 나눠 썼고, 번호를 **미리 갈라 잡아** 피했습니다.)
>
> **⚠ `.gitattributes`의 `merge=union`은 GitHub 서버 머지에는 안 먹습니다.** 그 규칙은
> `docs/adr/README.md`의 append-only 충돌을 없애라고 넣은 것인데, **로컬 머지에서만
> 적용됩니다.** 실제로 2026-08-13에 PR #161·#162가 GitHub 머지 버튼에서 *"Pull Request has
> merge conflicts"*로 거부됐습니다. 해법은 **로컬에서 `git merge origin/main` 한 번 하고
> 푸시**하는 것입니다 — 그러면 union이 먹어 충돌 없이 합쳐지고, 그다음 머지 버튼이 통합니다.
>
> **③ 지속계수가 문헌값으로 바뀌었습니다**([ADR-0039](docs/adr/0039-rim-persistence-from-literature.md)) —
> (0.6, 0.8, 1.0) → **(0.21, 0.62, 1.00)**, 중심 0.8 → **0.62**. 세 점 전부 출처가 있어
> 이 축에서 지어낸 숫자가 0개가 됐습니다. `backtest.py`에 따로 박혀 있던 **0.9도 같은 상수로
> 통일**했습니다(한 저장소에 w가 둘이었습니다). 실측: **w는 종합 판정을 거의 안 바꿉니다** —
> 1년 종합 IC가 후보 넷 전부 0.120이고 판정 등급 이동은 1.74%뿐입니다.
>
> **⚠ ADR-0039는 판정의 자를 결과를 본 뒤에 12개월 → 5년으로 옮겼습니다.** 그 이해충돌을
> 문서에 적어 뒀으니 인용하기 전에 읽으세요. `python scripts/check_rim_persistence.py
> --adjudicate-on 1년`이 최초 판정(기각)을 재현합니다.
>
> **⚠ ①의 ROE 구간이 7칸 → 8칸이 됐습니다**([ADR-0040](docs/adr/0040-close-the-open-top-roe-bucket.md)).
> 최상위가 `>15%`로 **열려 있어서** ROE가 15%를 넘으면 이익이 세 배가 돼도 적정 배수가
> 한 칸도 안 움직였습니다(삼성전자 17.6%→59.1%가 같은 칸 · 11종목 중 9곳). `0.25`에서
> 닫았습니다. 근거는 **미국 PBR의 leave-one-out 오차 −3.9%**(0.443→0.426 · R² 0.461→0.501)와
> `P/B`가 ROE에 선형이라는 것입니다. 한국은 중립이고(검증 IC 0.098 그대로) 화면은
> **한국 1.8% · 미국 4.8%** 바뀝니다 — **AAPL이 '적정 수준'→'저평가'로 넘어갑니다.**
> `LEG_MAE`도 함께 다시 쟀습니다. **①과 ⑤가 같은 회귀를 쓰므로 판정의 77%가 걸린 변경입니다.**
>
> **⚠ 새 ADR을 쓰기 전에 다른 워크트리의 `docs/adr/`를 확인하세요.**
> `check_adr_index.py`는 **브랜치를 가로질러 보지 못합니다** — 두 방이 같은 번호를 쓰고
> 문구만 다르면 중복으로 안 걸리고 그대로 머지됩니다. 실제로 0038이 그럴 뻔했습니다.
> (0038·0039·0040은 모두 main에 들어왔습니다 — PR #146·#147·#148.)
>
> **⚠ 앞선 PR과 같은 파일을 건드리면 main이 아니라 그 브랜치에서 따세요.**
> PR #146이 이걸로 충돌 났습니다 — #145에 의존한다는 것을 PR 본문에 적어 놓고도 브랜치는
> main에서 땄습니다. **"먼저 머지하세요"라고 쓰려는 순간이 곧 브랜치를 잘못 땄다는 신호입니다.**
> `git worktree add .claude/worktrees/<이름> -b <새브랜치> <의존브랜치>`
>
> **1·2·2b가 끝났고 3은 접혔습니다** — 근거 접기(PR #138) ·
> **② 역사적 밴드를 판정에서 뺐고**([ADR-0035](docs/adr/0035-drop-historical-band-from-the-verdict.md))
> **신뢰도 문턱을 독립분으로 고쳤습니다**([ADR-0036](docs/adr/0036-confidence-cap-measured-as-independence-fraction.md)).
> **판정은 이제 ①③⑤**이고 가중은 ① 38.5 · ③ 23.1 · ⑤ 38.5%입니다.
> **④와 시나리오는 자기 과거 PER을 계속 씁니다** — 3단계가 접혔으므로 `per_q_pricing`과
> 밴드 품질 장치도 그대로 남습니다(차트 전용이 되지 않습니다).
>
> **데이터를 다시 받지 않아도 됩니다** — `data/backtest/panel.parquet`이 저장소에 있고,
> **`raw/`도 이 PC에 있습니다**(KR 3,876파일 · US 4,170파일). 재수집 48분이 필요 없습니다.
>
> **패널은 재현됩니다**([ADR-0037](docs/adr/0037-the-panel-is-reproducible-the-inputs-were-not.md)).
> 같은 코드·같은 `raw/`로 두 번 만들면 7,656행 전 열이 최대차 0입니다.
> ADR-0035가 *"재현되지 않는다"*고 적은 절은 **틀렸고 ADR-0037이 정정했습니다** — 그때
> 비교한 두 패널은 코드도 `raw/`도 달랐습니다(1,288 → 1,292 종목).
>
> **⚠ 다만 두 패널을 비교하려면 `code_commit`과 `raw_fingerprint`가 둘 다 같아야 합니다.**
> 회귀 계수를 그 시점 종목 집합으로 적합하므로, **표본에 종목이 하나만 더 들어와도 ①과 ⑤가
> 전 종목에서 움직입니다.** 매니페스트가 이제 `raw/` 지문을 적습니다.
>
> **⚠ 로컬 테스트 2건이 실패한다는 안내는 이 PC에는 해당하지 않습니다.**
> `%TEMP%`가 `C:\Users\bogeun\...`로 ASCII이고 `node`도 설치돼 있어 **관문 11개가 전부
> 실제로 돕니다.** 그 안내가 가리키는 것은 한글 사용자명의 다른 환경입니다.

> **▶ A(창 5년 → 8년)와 B(백테스트)는 끝났습니다** —
> [ADR-0025](docs/adr/0025-normalize-window-eight-years.md) ·
> **[ADR-0028](docs/adr/0028-backtest-the-verdict-has-predictive-power.md)**.
> **C(EPV를 축으로 짓기)는 닫혔습니다** — ADR-0028이 종합에 못 보탠다고 쟀고
> (+0.004 · t=1.44), 재측정에서도 단독 0.122인데 종합 기여는 없습니다.
> 배경은 [`docs/HANDOFF-CONFIDENCE.md`](docs/HANDOFF-CONFIDENCE.md)에 남깁니다.
>
> **B의 숫자 하나가 정정됐습니다** —
> [ADR-0033](docs/adr/0033-unadjusted-splits-broke-mean-statistics.md): 분할·병합 조정이
> 누락된 주가가 **평균 기반 통계만** 무너뜨리고 있었습니다(IC는 멀쩡했습니다).
> 관문을 심은 뒤 B1 Q5−Q1은 **+6.1%**입니다. ADR-0028의 +13.1%는 표본이 달라
> 원인 분해를 못 했고, 못 했다고 적었습니다. **평균 기반 숫자를 인용할 때 이 ADR을 함께 읽으세요.**
>
> **⚠ B의 결론은 두 번 좁혀졌습니다. ADR-0028만 읽지 말고 0029·0030까지 읽으세요.**
> - **[ADR-0029](docs/adr/0029-one-year-was-the-wrong-ruler.md)** — 12개월은 이 도구의
>   주장에 맞는 자가 아니었습니다. ②만 빼고 **모든 축이 3~5년에서 좋아집니다**
>   (③RIM 0.070→0.152 · EPV 0.112→0.205). 그런데 **현행 종합은 5년에서 0.054로
>   무너집니다** — ②가 끌어내리기 때문입니다.
> - **[ADR-0030](docs/adr/0030-us-does-not-replicate.md)** — **미국에서는 재현되지
>   않습니다**(H1 t=0.92). 사전등록한 가설 넷 중 **부정 결과 둘만** 재현됐습니다.
>   ②를 빼자는 가설(H3)은 미국에서 **기각**됐습니다(t=0.35) — 한국 결과만 봤으면
>   ②를 뺐을 겁니다. **이것이 사전등록이 실제로 값어치를 한 자리입니다.**
>
> 정직한 한 문장: **한국 2017~2025 · 12개월 지평에서 확인됐고, 미국 같은 기간에서는
> 확인되지 않았습니다.** 화면에 이보다 강한 말을 쓰면 안 됩니다.
>
> **▶ B의 결과 — 이 도구가 채점받은 것은 이번이 처음입니다(ADR-0028).**
> 한국 1,292종목(**폐지 332곳 포함**) · 9개 시점(2017~2025) · 검증기간 2021~2025.
> - **판정에는 예측력이 있습니다** — 현행 4축 IC 0.103(t=4.66), 검증 5년 **전부** 양(+),
>   5분위 롱숏 +13.1%. '크게 저평가' +5.9% vs '크게 고평가' −3.5%.
> - **가중치는 결론을 바꾸지 않습니다**(25/25/15/25 0.103 = 동일가중 0.102).
>   ADR-0003이 순위만 인코딩하고 추정하지 않은 것이 옳았다는 **첫 증거**입니다.
> - **② 역사적 밴드는 아무것도 하지 않았습니다** — 단독 IC 0.004(t=0.19), 스프레드 −0.1%.
>   종합에서 뺀 차이가 t=−4.00으로 **이 연구에서 가장 뚜렷한 결과**입니다.
>   다만 ②를 빼는 조합은 사전등록 밖이라 **채택하지 않았습니다**(ADR-0028 결정 3).
> - **EPV는 종합에 보탤 것이 없습니다**(+0.004, t=1.44) — **C의 판단 근거입니다.**
> - **⑤의 창 8년은 5년과 구별되지 않습니다**(t=0.42). ADR-0025는 예측 관점에서 중립.
>
> **C를 여는 사람에게**: ADR-0024는 EPV가 커버리지를 넓힌다는 이유로 통과시켰는데,
> ADR-0028은 **종합의 예측력을 높이지 않는다**고 잽니다. 둘은 다른 질문이니
> (넓어지는가 vs 맞히는가) 어느 쪽을 살지 먼저 정하고 시작하세요.
>
> **ADR-0009의 벽 셋은 결국 셋 다 우리 쿼리의 한계였습니다.** 주가는 `period="5y"`
> 기본값이었고(`max`면 26년), 재무는 보고서를 2개만 받아 6년이었고(실제 13년),
> **시점별 유니버스도 `KRX-DELISTING`(4,172행)과 FDR로 복원됩니다.**
> ①의 "피어 목록이 없다"는 벽은 ADR-0014가 회귀로 바꾼 순간 이미 사라져 있었습니다 —
> 회귀에 필요한 것은 피어 목록이 아니라 그 시점의 횡단면입니다.
> 재현: `backtest_collect.py` → `backtest_panel.py` → `check_backtest_combos.py`.
>
> A에서 재고 알게 된 것 — **다음 작업의 전제가 되는 것들입니다**:
> DART 이력은 6년이 아니라 **중앙 13년**이고(`scripts/check_dart_depth.py`), 그 6년은
> `opendart.py`가 보고서를 2개만 받아 나온 값이었습니다. 창을 8년으로 올리는 대가는
> 거의 없었습니다 — 커버리지 62%→62%, 호출 3→5회(하루 첫 조회만, 캐시 24h).
> **이 변경은 KR 전용입니다**: 미국은 yfinance 연간 재무가 4행뿐이라 창을 올려도
> 같은 4년을 씁니다(AAPL은 변경 전후 숫자 하나까지 동일). 미국 이력을 늘리려면
> SEC EDGAR XBRL이 필요하고, 그것이 곧 B의 재료입니다.
> `scripts/check_window_tradeoff.py`가 창을 바꿔 재는 재현 명령입니다.
>
> 아래는 C의 배경입니다 — **판정 경로를 건드리는 작업입니다**
> (A는 창 상수와 데이터 깊이만 건드렸고, 축의 구성·가중치는 그대로입니다).
> 읽을 순서는 **0022 → 0023 → 0024**입니다 — ADR-0023이 미리 정한 관문으로 EPV를 **기각**했고,
> **ADR-0024가 그 관문의 상한(상관 0.5) 자체를 없앴습니다.** 재려던 해악(신뢰도 부풀림)을
> ADR-0022의 `n_eff`가 이미 값 매기고 있었기 때문입니다. **EPV는 새 기준을 통과합니다** —
> 커버리지 손실 0으로 절대가치 축이 KR 43%→60% · US 65%→89%로 늡니다.
> **ADR-0024는 이해충돌을 안고 있습니다**(기각이 동기였는데 새 기준이 EPV를 통과시킵니다).
> 그 문서에 방어 절을 따로 뒀으니 읽고 판단하세요.
> 363 passed, 99 subtests · CI 관문 11개 · ADR은 0031까지.
> 지난 브랜치들의 기록은 [`docs/HANDOFF.md`](docs/HANDOFF.md)에 있습니다.

## 프로젝트 개요
기본적 분석 기반 **주식 가치평가 대시보드**(한국+미국). 종목 하나를 넣으면 재무·주가·업종 데이터를
자동 수집해 "지금 주가가 적정한가, 아니라면 왜인가"를 판정·시각화한다.
프런트는 둘: **Meridian 웹**(`server.py` + `web/`, 실제 실행 진입점 — `대시보드실행.bat`)과
Streamlit(`app.py`). 두 프런트 모두 같은 분석 엔진(`src/analysis`)을 쓴다. 8개 탭
(기업·뉴스, 요약·판정, 주가차트, 밸류에이션, 재무, 업종비교, 자본비용(WACC), AI투자평가).
**백테스트 탭은 무기한 보류**(ADR-0009) — 버튼만 막았고 패널 마크업·`backtest.py`·
`renderBacktest()`는 그대로 살아 있다. 되살리려면 `web/stock.html`의 주석만 풀면 된다.
적정주가는 **다섯 가지 방법으로 계산하고 판정에는 셋(①③⑤)만 쓴다** — ① 업종 상대가치 ·
③ RIM · ⑤ 정규화 이익. **② 역사적 밴드는 ADR-0035에서 판정에서 뺐다**(차트로만 남는다).
이 줄에 '4방법'이라고 적혀 있었다 — ②가 빠진 뒤에도 갱신되지 않은 문장이었다.
①은 피어 중앙값이 아니라 **업종·규모·수익성 회귀**로 적정 배수를 구하며(ADR-0014),
⑤는 **8년 평균 순이익 × 그 회귀 적정 PER**이다(ADR-0015 · 창은 ADR-0025).
컨센서스 선행 이익(④)을 얹은 값은 **판정에 넣지 않고 화면에 나란히 병기**한다(ADR-0006).
두 값의 차이가 '지금 주가에 실린 시장의 실적 기대분'이다. + 증권가 컨센서스 교차검증
+ 비관/기준/낙관 시나리오.

## 실행 / 검증
- 실행: `pip install -r requirements.txt` → **`python server.py`** (→ http://localhost:5178).
  이 줄에 `streamlit run app.py`가 적혀 있었는데, **그쪽은 레거시로 동결된 구버전**이고
  같은 파일 위쪽이 이미 "실제 실행 진입점은 `server.py`"라고 적고 있었습니다.
  기동하면 백그라운드에서 캐시를 예열합니다(`_warm_cache` · ADR-0048) — 로그에 `[예열]`로 뜹니다.
- 헤드리스 검증(키 없이 됨): `python scripts/check_analysis.py KR 005930` / `US AAPL`,
  `python scripts/check_backtest.py KR 005930`
- 규모 편향 진단(수동·네트워크 필요, CI 아님): `python scripts/check_size_bias.py --limit 400`.
  판정이 '싸다'가 아니라 '작다'를 재고 있지 않은지 시총 구간별로 전수 측정한다.
- **커밋·PR 전에는 `python scripts/check_all.py`를 돌린다.** 이것이 CI 관문의 전부다 —
  `.github/workflows/quality.yml`을 직접 읽어 거기 적힌 명령을 그대로 실행하므로
  워크플로가 바뀌어도 어긋나지 않는다. 통과하면 CI도 통과한다.
  **관문 목록을 손으로 골라 돌리지 말 것.** 실제로 그래서 한 번 터졌다 — `check_design.py`만
  돌리고 "웹 변경은 괜찮다"고 판단했는데, 인라인 스타일 예산(`check_structure.py`)을
  넘긴 것을 CI가 잡았다. **한 관문이 통과했다는 사실은 다른 관문에 대해 아무 말도 하지 않는다.**
- 위 `check_all.py`는 **네트워크가 필요한 진단을 포함하지 않는다**(CI에도 없다).
  판정 로직을 건드렸으면 `check_analysis.py`·`check_warranted.py`·`check_normalized.py`·
  `check_valuation_basis.py`를 따로 돌려 실제 종목에서 확인할 것.
- **yfinance가 `possibly delisted; no price data found`를 뱉으면 상장폐지도 레이트리밋도
  아닐 수 있다.** 이 저장소는 한글 경로(`투자지표/`)에 있는데 yfinance가 쓰는 libcurl이
  **비ASCII 경로의 인증서 파일을 못 연다**. `src/data/ca_bundle.py`가 자동으로 우회하지만
  (ADR-0027), 같은 메시지를 다시 보면 **Yahoo를 의심하기 전에 이것부터 확인하라**:
  `python -c "from curl_cffi import requests as r; print(r.get('https://fc.yahoo.com', impersonate='chrome').status_code)"`
  — `curl: (77)`이면 이 문제다. 쿠키 캐시가 살아 있는 동안은 멀쩡하다가 **갱신 시점에
  전 종목이 한꺼번에** 죽는다.

## 구조 (핵심)
- `src/data/` — 데이터 수집. `models.py`(시장 무관 표준 모델 `CompanyData`), `base.py`(yfinance),
  `opendart.py`(한국 공시 원본), `naver.py`, `news.py`, `gemini.py`(AI), `kr_provider.py`/`us_provider.py`,
  `parallel.py`(서로 모르는 호출을 겹치는 `gather` · ADR-0045),
  `cache.py`(파일 캐시 + `single_flight_memo` — **같은 것을 동시에 두 번 만들지 않는다** · ADR-0047).
- `src/web/` — `serialize.py`(분석 결과 → 프런트 JSON), `prewarm.py`(예열 목록과 동작 · ADR-0048).
  **쇼케이스 목록은 여기 한 벌뿐입니다** — `scripts/prewarm_cache.py`는 그 얇은 CLI이고,
  테스트가 스크립트에 목록이 되살아나는 것을 막습니다.
- `src/analysis/` — **순수 함수**로 작성(입력=CompanyData, 부작용 없음). `indicators.py`, `scoring.py`,
  `capital_cost.py`(베타·하마다·WACC), `valuation.py`(적정주가 5방법 계산 · **판정은 ①③⑤ 종합** · ②④는 병기·참고, ADR-0006·0035), `backtest.py`, `ai_analysis.py`.
- `src/ui/` — `charts.py`(Plotly), `components.py`(포맷터·배지). `app.py`가 엔트리.

## 코딩 규칙
- 주석·UI 문구는 **한국어**. 기존 코드의 톤·밀도를 따를 것.
- 분석 로직은 `src/analysis/`의 순수 함수로. 시장이 늘면 `src/data/`에 provider만 추가.
- 무료 데이터라 **결측이 흔함** → 값이 없으면 `None` 처리하고 절대 크래시 내지 말 것(N/A 표기).
- 차트 색은 `src/ui/components.py`의 검증된 팔레트(Streamlit)와 `web/assets/meridian.css`의
  `--dv-*` 토큰(Meridian 웹)만 사용. **색은 면과 선에 쓰고, 값을 리터럴로 적지 않는다.**
- **판정에는 색을 쓰지 않는다**(R4). "저평가/고평가"는 우리가 내린 판단이고 등락률·수익률은
  숫자의 부호인데, 둘을 같은 색으로 칠하면 초록을 본 사람이 "싸다"인지 "올랐다"인지 갈라낼 수
  없다(게다가 이 둘은 자주 반대 방향이다). 판정 헤드라인·배지·눈금 라벨은 **무채 잉크**로만
  쓰고 세기는 진하기로 말한다. 방향은 문자와 눈금 위 위치가 말한다.
  초록(`--dv-positive`)·클레이(`--dv-negative`)는 **숫자의 부호 전용**이다.
- 글자로 쓰는 색은 지면 위에서 WCAG AA(4.5:1)를 넘겨야 한다. 면·선으로만 쓰는 값과 토큰이
  다르다(`--dv-green`은 면, `--dv-positive`는 글자). 확인: `python scripts/check_design.py`
- 새 파이썬 의존성을 추가하면 `requirements.txt`도 갱신.

## 브랜치 · PR 규칙 (중요)

로컬 작업과 GitHub의 `@claude`가 각자 브랜치를 만들다 보니, base(합쳐질 목적지)를 잘못 잡아
**변경이 main에 닿지 못하고 사라지는 사고가 반복**됐다(#31·#36). 머지 버튼은 정상으로 보이고
PR 상태도 MERGED가 되기 때문에 눈으로는 성공한 것처럼 보인다. 아래를 지킨다.

- **브랜치는 항상 최신 main에서 딴다.** 작업 전 `git fetch origin` →
  `git switch -c <새브랜치> origin/main`. 다른 작업 브랜치 위에서 그대로 파생하지 않는다.
- **PR의 base는 언제나 `main`.** 다른 PR 브랜치를 base로 쓰지 않는다.
- 불가피하게 스택을 쌓아야 하면 PR 본문 **첫 줄에** 의존 PR 번호와 머지 순서를 밝히고,
  의존 PR이 머지되는 즉시 이 PR의 base를 `main`으로 되돌린다.
- **내가 만들지 않은 브랜치는 force-push하지 않는다.** 자기 브랜치도 `--force-with-lease`만 쓴다.
- 머지 뒤에는 반드시 실제 반영을 확인한다: `git fetch origin && git log --oneline origin/main -5`
- 위 규칙은 `.github/workflows/pr-base-guard.yml`이 PR마다 자동 검사한다(죽은 base면 CI 실패).

**ADR 인덱스는 충돌하지 않는다 — 손으로 풀지 말 것.** `docs/adr/README.md`는 PR마다 자기
ADR 한 줄을 표 끝에 붙이는 append-only 표라, 기본 머지 전략이 이걸 매번 충돌로 봤다
(#99·#100·#101에서 세 번 반복). `.gitattributes`의 `docs/adr/README.md merge=union`이
양쪽 줄을 모두 남겨 충돌 자체를 없앤다. **표에 줄을 넣을 때 순서를 맞추려고 애쓰지 않아도
된다** — union이 번호순을 흐트러뜨릴 수 있고, `scripts/check_adr_index.py`가 그건 알림만
한다. 실패시키는 건 셋뿐이다: 같은 줄이 두 번(union이 프로즈까지 겹쳤을 때) · 파일은
있는데 표에 없음 · 표에는 있는데 파일이 없음.
  `.gitattributes`는 **머지를 수행하는 브랜치의 작업 트리**에 있어야 적용된다(main에만 있으면
  안 된다). 지금 main에서 딴 브랜치는 자동으로 갖는다 — 이 규칙이 들어오기 전에 만든
  브랜치만 `git checkout origin/main -- .gitattributes`를 한 번 하면 된다.

## 보안 (중요)
- **API 키를 절대 코드/커밋에 넣지 말 것.** 키는 `.streamlit/secrets.toml`(=`.gitignore`로 제외)
  또는 환경변수(`OPENDART_API_KEY`, `GEMINI_API_KEY`)로만 읽는다.
- 키가 없어도 앱이 동작해야 함(폴백 유지).

## 성격
투자 조언이 아니라 **학습·분석 보조 도구**. 새 기능에도 "판단 근거를 보여주되 단정하지 않는다"는
톤을 유지하고, AI 생성 결과에는 면책 문구를 붙인다.
