> ## ▶ 다음 사람이 이어받을 일 — **미국 방법 상관 측정**
>
> **한국은 끝났다(ADR-0022 채택·구현 완료). 미국만 남았다.**
>
> `scripts/check_confidence.py`가 표본 틀로 `get_kr_listing()`을 쓴다 — 한국 상장 전체
> 목록이라 **미국을 못 돈다.** 그래서 `warranted.METHOD_RHO`에 `"KR"`만 있고, 미국
> 종목은 `effective_axes()`가 `capped=False`를 돌려 **상한이 아예 안 걸린다.**
> 미국 판정의 신뢰도는 지금도 옛 산식 그대로다.
>
> **왜 급한가** — ADR-0022의 성패 판정 하나가 *"AAPL이 더 이상 '높음'이 아니어야 한다"*
> 인데, 미국 표가 없어서 **확인하지 못했다.** 그 기준은 미달인 채로 남아 있다.
> ADR-0015가 AAPL을 실례로 들어 이 작업 전체의 출발점이 된 종목이라 상징적이기도 하다.
>
> ### 할 일 (순서대로)
>
> 1. **`check_confidence.py`에 시장 인자를 붙인다.** 지금 KR 전용으로 굳어 있는 곳은
>    두 군데다 — `get_kr_listing()` 표본 틀과 `KRProvider().load()` 호출.
>    미국 표본 틀은 `src/data/universe_multiples.py`의 `collect_us()`가 쓰는 S&P 1500
>    목록이 이미 있다(위키백과 스키마). 로더는 `src/data/us_provider.py`의 `USProvider`다
> 2. **돌려서 상관표를 얻는다** — `python scripts/check_confidence.py US --limit 200`
> 3. **`METHOD_RHO["US"]`에 붙인다.** 스크립트가 붙여넣을 줄을 그대로 찍는다(섹션 E)
> 4. **AAPL로 성패를 판정한다** — `python scripts/check_analysis.py US AAPL`.
>    신뢰도가 '높음'이 아니어야 한다
>
> ### 미리 알아 둘 것
>
> - **한국 결과를 미국에 복사하지 말 것.** 같은 실수를 오차표에서 이미 봤다 —
>   PSR MAE가 한국 0.897 대 미국 0.656으로 크게 다르다. 상관도 다를 이유가 충분하다
> - **`ρ̄`가 한국과 반대로 나올 수 있다.** 한국에서는 ③RIM↔① 이 +0.787로 가장 겹쳤는데,
>   이건 예측이 아니라 **측정 뒤에 붙인 설명**이다. 미국에서 다르게 나와도 놀랄 일이 아니다
> - **yfinance가 자주 막힌다**(`401 Invalid Crumb`). 재시도 없이 돌리면 표본이 0이 된다 —
>   `_one()`이 이미 재시도·백오프를 갖고 있고 `MAX_WORKERS`가 2다. 더 올리지 말 것
> - **표본은 시총 5분위 층화로 뽑는다.** 상위 N만 쓰면 대형주만 잡혀 상관이 왜곡된다
>
> ### 그다음이 EPV다 (ADR-0016 결정 2)
>
> 미국까지 끝나면 4번(EPV)으로 간다. **EPV를 넣으면 상관을 다시 재야 한다** —
> `effective_axes`가 모르는 쌍을 만나면 상한을 통째로 끄기 때문에(ADR-0011),
> 재지 않고 넣으면 신뢰도 상한이 **조용히 무력화된다.** `test_unmeasured_legs_are_named_not_invented`
> 와 `test_unknown_pair_disables_the_cap`이 그 동작을 못박아 뒀다.

---

# 작업 인계 — 3번 **신뢰도 산식** (ADR-0016 결정 3)

> 2026-08-04 기준. 이어받는 사람이 **이 문서 하나만** 읽고 시작할 수 있게 쓴다.
> **아직 아무것도 구현되지 않았다.** 코드도 ADR도 없는 착수 전 인계문이다.

## 한 줄 요약

**신뢰도를 `std(중심값)/|mean|` 하나로 내는데, 이 산식이 두 가지 이유로 거짓말을 한다 —
축이 서로 상관되면 부풀려지고, 방법 수가 종목마다 1~4개인데 문턱은 하나다.**
ADR-0016이 "축(EPV)을 늘리기 전에 이것부터"라고 순서를 못박았다.

## 지금 어디까지 와 있나

| | |
|---|---|
| main | `d5f639d` — ADR-0016·0017·0018(PR #107) 뒤로 #109~#118이 더 들어왔다 |
| 이 작업 | **착수 전.** 코드 0줄, ADR 없음 |
| 테스트 | `319 passed, 80 subtests` |
| CI 관문 | **11개** (`scripts/check_all.py`) |
| 열려 있는 PR | 없다 — 다만 **방금 머지된 둘이 숙제를 남겼다**(아래) |

## 방금 머지된 PR 둘이 이 작업에 숙제를 남겼다

### #115 (규모 항 스플라인, ADR-0021) — **오차표 재측정이 이 작업의 범위다**

`warranted.py`의 규모 항이 직선에서 **스플라인**으로 바뀌었다. 직선을 꼬리까지 연장해
삼성전자 적정 PBR을 17.96배(실제 3.30배)로 계산하던 것을 고친 변경이다.

**그래서 `LEG_MAE`(ADR-0017의 실측 오차표)가 지금 옛 함수형의 값이다.** PR #115가 그
사실을 직접 적고 *"다음 작업(신뢰도 산식)이 오차 프레이밍을 정면으로 다루므로 거기서
함께 재는 것이 맞다"*며 넘겼다.

화면의 오차 폭(ADR-0017 콜아웃)이 `LEG_MAE`를 그대로 읽는다 — 즉 **지금 화면이 낡은
숫자를 말하고 있다.** 이 작업에서 함께 재라.

### #118 (EBITDA 위생) — 축 하나의 입력이 정상화됐다

EV/EBITDA 다리의 입력이 고쳐졌다(인텔 144배 → 31.7배 · NAVER도 같은 증상이었다).
①의 다리 값이 바뀌었으니 **방법 간 편차도 함께 바뀌었다.** 이 수정 **전에** 잰 산포·상관
수치가 어딘가 남아 있으면 그대로 인용하지 말고 다시 재라.

## 왜 4번(EPV)보다 먼저인가

ADR-0016 결정 3의 원문이다. 순서를 정한 것은 이 문장이다:

> **신뢰도 산식** — `std(중심값)/|mean|`은 축이 상관되면 편차가 작아 **부풀려진다**
> (ADR-0012·0014·0015가 각각 경고했다). 방법 수가 종목마다 1~4개인데 문턱은 하나다.
> **축을 더하면 이 지표가 더 망가지므로 축보다 먼저다.**

EPV는 RIM과 같은 칸(이익 기반 영구가치)이라 서로 상관될 가능성이 높다. 상관된 축을
하나 더 얹으면 편차가 더 줄고 신뢰도는 더 부푼다. **순서를 뒤집으면 지표가 망가진 뒤에
고치게 된다.**

ADR-0018도 같은 말을 대안 절에 적어 두고 미뤘다:

> **신뢰도 산식에 상관을 반영한다.** 근본적인 해법이고 실제로 해야 할 일이다. 그러나
> 산식 변경은 판정 화면 곳곳의 입력을 바꾸고 별도 측정이 필요하다 — **별개 ADR로 다룬다.**

## 현행 산식 — 전부 여기 있다

`src/analysis/valuation.py:1010`

```python
if len(mids) >= 2 and res.fair_mid:
    disp = float(np.std(mids) / abs(np.mean(mids)))
    res.dispersion = disp
    res.confidence = "높음" if disp < 0.15 else "중간" if disp < 0.35 else "낮음"
    ...
else:
    res.confidence = "낮음"   # 방법이 1개뿐
```

`mids`는 `[e.mid for e in basis]`이고 `basis`는 판정에 실제로 들어간 방법들이다
(①②③⑤ 중 선 것. 전부 빠지면 ④ 하나).

## 알려진 문제 둘 — 근거가 각각 있다

### 문제 1. 축이 상관되면 부풀려진다

편차가 작은 것이 **'확실하다'가 아니라 '같은 것을 두 번 쟀다'**일 수 있다.
세 ADR이 각각 경고했는데 산식은 그대로다.

- **ADR-0015 · AAPL 실례** — ①과 ⑤가 **같은 적정 PER 27.8배**를 쓴다. 그래서 값이 가깝고
  (237 대 191) 신뢰도가 **'높음'**으로 나온다. 두 축이 아니라 사실상 한 축이다
- **ADR-0014** — ①과 회귀(D2)의 괴리율 상관이 **0.839**(부호 일치 82%). 그래서 D2를
  ①과 **병기하지 않고 대체**했다. 병기하면 "정보는 안 늘고 편차만 줄어 신뢰도가 부푼다"
- **ADR-0012** — ②와 ④가 **같은 배수**(자기 과거 PER 중앙값)에 각각 TTM EPS와 컨센서스
  EPS를 곱한 값이다. "서로 다른 관점이 아니라 같은 관점의 과거판·미래판"

**구조적 원인은 ADR-0014가 이미 규명해 뒀다** — 괴리율 = `log(적정배수) − log(현재배수)`인데
`log(현재배수)`의 분산이 적정배수 분산보다 2배 이상 커서 괴리율의 **55~60%를
`−log(현재배수)`가 설명한다.** 배수 기반 방법을 몇 개 쌓든 이 항을 공유한다.
즉 **①②⑤는 원리적으로 독립일 수 없다.**

### 문제 2. 방법 수가 1~4개인데 문턱은 하나다

2개짜리 표준편차와 4개짜리를 `0.15 / 0.35` 같은 자로 잰다.

`np.std`는 기본이 **ddof=0(모집단)**이라 n=2면 `|a−b|/2`가 된다. 표본 수가 늘수록
같은 산포도라도 값이 달라지는데 보정이 없다. **n별로 문턱이 어떻게 달라져야 하는지가
이 작업의 핵심 중 하나다.**

### (관찰) 셋째 — 분모가 판정에 쓰는 값이 아니다

코드를 읽다 나온 것이고 **ADR에 적힌 적이 없다. 의도인지 아닌지 확인이 필요하다.**

판정은 `_weighted(basis)`의 **가중평균** `fair_mid`로 내는데, `disp`는
`np.std(mids) / abs(np.mean(mids))` — 분자도 분모도 **가중치를 무시한 단순 통계**다.
`METHOD_WEIGHTS`가 방법마다 다른데 산포는 전부 동일가중으로 잰다.
의도된 단순화일 수 있으니 **먼저 물어보고 재라.**

## ADR-0018이 만들어 둔 어색함 — 여기서 풀린다

**신화콘텍(187270)은 신뢰도가 "중간"인데 화면은 "이 판정은 전부 시장 배수에 기대고
있습니다"라고 말한다.** 둘 다 참인데 나란히 서면 모순처럼 보인다.

절대가치 축이 하나도 없는 종목이 실측 **53%**이고, 절대가치 실효 비중의 **중앙값은 0.0%**다
(141종목). 지금 신뢰도는 이 사실을 전혀 안 본다 — 방법 간 편차만 본다.

## 이미 있는 재료 — 새로 만들지 말 것

산식이 쓸 수 있는 값들이 **이미 계산돼 있다.**

| 값 | 위치 | 무엇인가 |
|---|---|---|
| `shared_multiple_share` | `valuation.py:215` | ②④가 **같은 배수 하나**에 실제로 얼마나 매달렸나(0~1). 명목 가중이 아니라 중심값 크기까지 반영한 **실효 의존도** |
| `intrinsic_share` / `relative_share` | ADR-0018 | 절대/상대 실효 비중. `intrinsic_share == 0`이면 절대가치 축이 하나도 없다 |
| `INTRINSIC_METHODS` / `RELATIVE_METHODS` | `valuation.py:100` | 방법의 성격 분류. 진단과 화면이 같은 분류를 본다 |
| `leg_error` | ADR-0017 | ①에 쓴 다리들의 **실측 오차**와 안전마진. 미측정 다리는 비워 둔다 |
| `relative_leg_sensitivity` | ADR-0014 | ①이 다리 하나에 얼마나 매달렸나 |

`shared_multiple_share`가 특히 참고할 만하다 — **"공유 의존도"를 이미 한 번 정의해 본
전례**다. 다만 지금은 ②④(병기값)에만 붙어 있고 판정 종합에는 안 쓰인다.

## 먼저 재라 — 이 저장소의 관례다

**ADR-0016이 앞선 인계문의 결론을 실측으로 뒤집은 것이 바로 직전 사례다.**
HANDOFF-NEXT가 "DCF가 절대가치 비중을 올리는 유일한 방법"이라고 했는데, 150종목을 재보니
DCF의 고유 기여는 3곳(2%)이었고 EPV가 더 넓었다.

그러니 산식을 고르기 전에 **진단 스크립트부터** 짓는 것을 권한다
(`scripts/check_dcf_viability.py`가 형식의 본보기다). 재야 할 것:

1. **축 쌍별 상관** — ①②③⑤의 중심값(또는 괴리율)이 실제로 얼마나 상관되나. 전 종목
2. **`disp` 분포를 방법 수(n)별로** — n=2와 n=4의 분포가 실제로 얼마나 다른가.
   지금 문턱 `0.15/0.35`가 n별로 무엇을 뜻하는지
3. **'높음'인데 축이 하나뿐인 종목의 비율** — AAPL 같은 경우가 얼마나 흔한가
4. **신뢰도와 `intrinsic_share`의 교차표** — 신화콘텍 같은 모순 조합이 몇 %인가
5. **`LEG_MAE` 다시 재기** — #115가 규모 항 함수형을 바꿨으므로 ADR-0014의
   leave-one-out을 새 함수형으로 다시 돌려야 한다. 위 '방금 머지된 PR 둘' 절 참조

**한 종목으로 일반화하지 말 것.** 이 저장소에서 두 번 그 실수를 했고 둘 다 HANDOFF.md
말미에 기록돼 있다.

## 안 정해진 것 — 설계 판단이 필요하다

이 문서는 **문제만 정리했다.** 아래는 정해진 바 없다.

- **상관을 어떻게 반영하나** — 유효 표본 수(effective N)로 깎을지, 축 성격(절대/상대)으로
  묶을지, 공유 항(`−log(현재배수)`)을 빼고 잴지
- **문턱을 n별로 나눌지, 산식 자체를 n에 불변으로 만들지**
- **신뢰도를 한 축으로 유지할지, 두 축(산포 + 독립성)으로 나눌지** — ADR-0018이 이미
  "무엇에 기대는가"를 별도로 내고 있어 겹칠 수 있다
- **기존 '높음/중간/낮음' 3단계를 유지할지**

## 손대기 전에 알아야 할 것

- **브랜치는 항상 최신 main에서 딴다.** `git fetch origin` →
  `git switch -c <새브랜치> origin/main`. PR base는 **언제나 `main`**
- **다음 ADR 번호는 0022부터다.** 0021까지 쓰였다. 실제로 #115와 #117이 **둘 다 0020을
  써서 겹친 적이 있으니**(`check_adr_index.py`가 "같은 번호가 두 줄"로 잡아냈다),
  번호를 고르기 전에 `docs/adr/`와 **열려 있는 PR을 함께** 확인하라
- **커밋 전에 `python scripts/check_all.py`.** 관문을 손으로 골라 돌리지 말 것 —
  실제로 그래서 한 번 터졌다
- **`stock.js` 인라인 스타일 예산이 308/308으로 꽉 찼고 규칙이 "내려가기만 한다."**
  화면을 건드리면 **CSS 클래스로 올려야 한다**
- **신뢰도는 화면 여러 곳의 입력이다.** 산식을 바꾸면 배지·경고 문구·`check_*` 진단이
  함께 움직인다. ADR-0018이 "판정 화면 곳곳의 입력을 바꾼다"고 미룬 이유가 이것이다
- **판정에는 색을 쓰지 않는다(R4).** 신뢰도 배지도 무채 잉크로만, 세기는 진하기로

## 함께 정리할 것 — 옛 PSR 경고가 한국 수치를 미국 종목에 쓴다

`valuation.py:820`의 *"PSR이 실측 오차가 가장 커서(전 종목 검증에서 ±150% 수준)"* 경고가
**시장 구분 없이** 뜬다. ±150%는 **한국** PSR MAE 0.921의 값이고 미국 PSR은 잰 적이 없다
(`LEG_MAE["US"]`에 `pbr`·`per`뿐이다).

바로 옆 ADR-0017이 *"한국 값을 대신 쓰는 것은 지어내는 것"*이라며 금지한 그 행위라,
WBD 화면에 "측정된 적이 없습니다"와 "±150% 수준"이 **몇 줄 간격으로 나란히 선다**(실화면 확인).
PR #107 범위 밖이라 남겨 뒀다 — **이 작업이 오차 프레이밍을 정면으로 다루므로 여기서
함께 정리하는 것이 맞다.**

## 이어받는 법

```bash
git fetch origin
git switch -c <새브랜치> origin/main
.venv/Scripts/python.exe -m pytest tests/ -q        # 319 passed, 80 subtests
```

### 읽을 순서

1. **이 문서**
2. `docs/adr/0016-no-dcf-epv-instead.md` **결정** 절 — 순서를 정한 문서
3. `docs/adr/0018-declare-what-the-verdict-rests-on.md` **검토한 대안** 절 — 왜 미뤘나
4. `docs/adr/0015-normalized-earnings-axis.md` **한계** 절 — AAPL 실례
5. `src/analysis/valuation.py:1010` — 산식 전부

### 검증 명령

```bash
.venv/Scripts/python.exe scripts/check_all.py                  # CI 관문 전부(11개)
.venv/Scripts/python.exe scripts/check_analysis.py KR 005930   # 실제 종목
.venv/Scripts/python.exe scripts/check_analysis.py US AAPL     # ①⑤가 같은 배수를 쓰는 예
.venv/Scripts/python.exe scripts/check_valuation_basis.py      # 상대/절대 비중
```

## 더 나은 의견이 있으면

환영한다. 다만 이 문서가 인용한 수치를 뒤집는 주장이라면 **다시 재고 근거를 남겨 달라.**
이 저장소는 ADR에 "무엇을 정했나"뿐 아니라 **"무엇을 재서 그렇게 정했나"와 "무엇을 안 했고
왜 안 했나"**를 함께 적는 관례가 있다(`docs/adr/` 참조).

**이 문서에서 실제로 잰 것은 테스트 수(319) · CI 관문 수(11) · 현행 산식 코드뿐이다.**
나머지 수치는 전부 기존 ADR과 PR 본문에서 인용했고, 위 '먼저 재라' 절의 다섯 가지는
**아직 아무도 재지 않았다.**
